<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/default_background"
    android:foreground="?android:attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/channel_list_item_outside_spacing"
        android:gravity="center">

        <ImageView
            android:id="@+id/channel_list_channel_icon_image_view"
            android:layout_width="@dimen/channel_list_item_icon_size"
            android:layout_height="@dimen/channel_list_item_icon_size"
            android:layout_margin="@dimen/channel_list_item_outside_spacing" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/channel_list_channel_name_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/channel_list_item_inside_spacing"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/channel_list_name_text_color"
                tools:text="Channel Name"/>

            <TextView
                android:id="@+id/channel_list_channel_url_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/channel_list_url_text_color"
                android:textStyle="italic"
                tools:text="channel_url"/>

        </LinearLayout>

    </LinearLayout>

</FrameLayout>